{
    acme_dns cloudflare {{ cloudflare_token }}
    servers {
        # Cloudflare
        trusted_proxies static private_ranges 173.245.48.0/20 103.21.244.0/22 103.22.200.0/22 103.31.4.0/22 141.101.64.0/18 108.162.192.0/18 190.93.240.0/20 188.114.96.0/20 197.234.240.0/22 198.41.128.0/17 162.158.0.0/15 104.16.0.0/13 104.24.0.0/14 172.64.0.0/13 131.0.72.0/22 2400:cb00::/32 2606:4700::/32 2803:f800::/32 2405:b500::/32 2405:8100::/32 2a06:98c0::/29 2c0f:f248::/32 
    }

    crowdsec {
        api_url http://localhost:9000
        api_key 7CavzX8oed058vcEgFKNzyhGsLoAa8mO4TroaUyH4WM
        ticker_interval 15s
        appsec_url http://localhost:7422
        #disable_streaming
        #enable_hard_fails
        appsec_timeout 120s
    }
}

(authentik_forward_auth) {
    reverse_proxy /outpost.goauthentik.io/* {{ auth.address }}
    forward_auth {{ auth.address }} {
        uri /outpost.goauthentik.io/auth/caddy

        # capitalization of the headers is important, otherwise they will be empty
        copy_headers X-Authentik-Username X-Authentik-Groups X-Authentik-Email X-Authentik-Name

        # optional, in this config trust all private ranges, should probably be set to the outposts IP
        trusted_proxies private_ranges
    }
}

(cors) {
    @cors_preflight {
        method OPTIONS
    }

    header Access-Control-Allow-Origin "{args.0}"
    header Access-Control-Allow-Methods "GET, HEAD, POST, PUT, PATCH, DELETE, OPTIONS"
    header Access-Control-Allow-Headers "Accept, Accept-Encoding, Authorization, Cache-Control, Content-Type, Content-Length, Origin, X-Real-IP, X-CSRF-Token"
    header Access-Control-Max-Age "3600"

    respond @cors_preflight 204
}

{% if root_domains is defined %}
{% for root_domain in root_domains %}
*.{{ root_domain }} {
}

{% endfor %}
{% endif %}
{% for site in sites %}
{% if site.external_port is defined %}
{{ site.subdomain if site.subdomain is defined else site.service }}.{{site.root_domain if site.root_domain is defined else root_domains[0] }}:{{ site.external_port }} {
{% else %}
{{ site.subdomain if site.subdomain is defined else site.service }}.{{site.root_domain if site.root_domain is defined else root_domains[0] }} {
{% endif %}
    log {
        level INFO
        output file /var/log/caddy/{{ site.full_name if site.full_name is defined else site.service }}.log {
            roll_size 10MB
            roll_keep 10
        }
    }

    route {
{% if 'disable_crowdsec' not in site or ('disable_crowdsec' in site and not site.disable_crowdsec)  %}
        crowdsec

{% endif %}
{% if 'disable_appsec' not in site or ('disable_appsec' in site and not site.disable_appsec)  %}
        appsec

{% endif %}
        header {
            X-XSS-Protection "0"
{% if 'frame_options' in site %}
{% if site.frame_options != "do-not-alter" %}
            X-Frame-Options "{{ site.frame_options }}"
{% endif %}
{% else %}
            X-Frame-Options "DENY"
{% endif %}
            X-Content-Type-Options "nosniff"
            -Server
            -X-Powered-By
            -Last-Modified
{% if 'custom_rules_header' in site %}
            {{ site.custom_rules_header  | indent(12) }}
{% endif%}
        }
{% if 'authentik_forward_auth' in site %}
{% if site.authentik_forward_auth %}
        import authentik_forward_auth
{% endif %}
{% endif %}
{% if 'custom_rules_block' in site %}

        {{ site.custom_rules_block | indent(8) }}
{% endif%}

{% if site.internal_port is defined %}
        reverse_proxy {{ site.ip }}:{{ site.internal_port }} {
{% else %}
        reverse_proxy {{ site.ip }}:{{ site.port }} {
{% endif %}
            header_up X-Real-IP {http.request.header.Cf-Connecting-Ip}
{% if 'custom_rules_reverse_proxy' in site %}

{{ site.custom_rules_reverse_proxy | indent(12) }}
{% endif%}
        }
    }
}

{% endfor %}
